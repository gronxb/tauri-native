{
  "name": "tauri-native-monorepo",
  "version": "0.0.1",
  "private": true,
  "packageManager": "nub@0.8.3",
  "workspaces": [
    "packages/*",
    "examples/*"
  ],
  "scripts": {
    "build": "nub --cwd packages/cli run build && nub --cwd packages/react-native run build && nub --cwd packages/lynx run build && nub --cwd examples/tauri run build && nub --cwd examples/lynx run build",
    "build:native:ios": "nub --cwd packages/cli run build && node packages/cli/dist/index.mjs build ios --tauri-dir examples/tauri/src-tauri --output-dir examples/react-native/ios/tauri-native",
    "build:native:lynx:ios": "nub --cwd packages/cli run build && node packages/cli/dist/index.mjs build ios --tauri-dir examples/tauri/src-tauri --output-dir examples/lynx/ios/tauri-native",
    "prebuild:ios": "nub --cwd examples/react-native run prebuild",
    "prebuild:clean:ios": "nub --cwd examples/react-native run prebuild:clean:ios",
    "pods:lynx:ios": "nub --cwd examples/lynx run pods",
    "test": "nub run test:rust && nub --cwd packages/cli run test && nub --cwd packages/react-native run test && nub run build && nub run typecheck && nub run test:packages",
    "test:packages": "node scripts/verify-packages.mjs",
    "test:rust": "cargo test --workspace",
    "typecheck": "nub --cwd packages/cli run typecheck && nub --cwd packages/react-native run typecheck && nub --cwd packages/lynx run typecheck && nub --cwd examples/react-native run typecheck && nub --cwd examples/tauri run typecheck && nub --cwd examples/lynx run typecheck"
  }
}
