# 1.0.0-rc.0 candidate

This is an experimental, reviewable candidate for the source-preserving workflow. It is not a stable 1.0 release or a statement that adoption and physical-device acceptance have completed. Source versions and prepared tarballs do not establish npm publication.

## Included behavior

- Install the CLI in an ordinary Tauri project, inspect its registered commands and export portable iOS/Android binaries, frontend assets and generated TypeScript contracts.
- Consume copied artifacts through React Native, Expo CNG or Lynx, without producer source or Rust tools in the native host build.
- Use ABI 2 nonblocking invocation and explicit cancellation/document/runtime lifetime semantics, with scoped frontend interactions and the verified application-data-directory adaptation.
- Diagnose compatibility and artifact problems, refresh exports incrementally, and watch ordinary source changes.

The [compatibility contract](../compatibility.md) defines supported signatures/APIs and explicit exclusions. The [Fieldnotes walkthrough](../examples/fieldnotes.md) and [migration guide](../migration.md) show the actual installation and host APIs. The producer keeps its ordinary desktop frontend and Rust source.

## Candidate evidence

[PR #37](https://github.com/gronxb/tauri-native/pull/37) merged as `05cb528`. Its [complete validation at `eb1fab837fc4f52747a00c522e75d63a403463c7`](https://github.com/gronxb/tauri-native/actions/runs/34111222423) passed the producer, both native platforms and candidate aggregation.

| Gate | Status |
| --- | --- |
| CLI, RN and Lynx unit/package verification at 1.0.0-rc.0 | Passed in the hosted producer |
| Receipt rejection for failed/incomplete checks, stale commits and changed tarballs | Passed in the hosted producer |
| Actual npm dry-run for all three packed packages | Passed; explicit experimental channel/public access, identical validated tarball hashes |
| Install → both exports → uninstall → normal desktop Release build | Passed in the hosted producer; no remaining authored-source changes |
| iOS candidate acceptance at `eb1fab8` | Passed: 13 required JUnit scenarios with matching receipts and package hashes |
| Android candidate acceptance at `eb1fab8` | Passed: 13 required JUnit scenarios, copied standalone APK execution with 16 KB pages, and minified Lynx |
| Complete native CI and matching candidate at `eb1fab8` | Passed: all four jobs; downloaded candidate independently verified |
| Physical iOS and Android RC execution | Pending |
| Two independent documentation-only onboarding records | Pending |
| Stable publication decision | Pending |

The downloaded iOS and Android evidence contains exactly 13 JUnit scenarios per platform with zero failures, errors or skips: original and Rust-refreshed Fieldnotes in RN/Expo/Lynx, pending navigation in all three hosts, and RN/Lynx async and scoped-view lifetimes. Native receipts and their separate feature reports match the producer and all package hashes. The six pending-navigation observations per platform ranged from 3.991–12.066 seconds on iOS and 4.112–8.060 seconds on Android during an injected 20-second Rust delay, including automation overhead.

The producer's installed CLI check permitted only the JavaScript manifest and lockfile installation diff, preserved the installed producer during both exports, and left a successful ordinary desktop Release build after CLI/output removal. It is automated maintainer evidence, not independent onboarding. All three iOS slices and four Android ABIs were built and inspected; execution covers the documented arm64 iOS simulator and API 36 x86_64 Android emulator with 16 KB pages. Physical-device execution is not covered.

The final workflow candidate verifies the source commit, required successful job results, package hashes and embedded names, versions and publish settings. Its 1.0.0-rc.0 tarballs are:

| Package | SHA-256 |
| --- | --- |
| `@tauri-native/cli` | `d5cac46f7f0ca6e195f6b1e71292300b2740713d4f8f1bede5b19c8dd08a3513` |
| `@tauri-native/react-native` | `16e3b9a6fcd17cca436bca494edf86e797f1cf4aca8e8401db1e3aab5bea7b5f` |
| `@tauri-native/lynx` | `4d292966a4ea06595b70e53a7549e908146228a071aa2d74fb497e9ea542ce04` |

The [validation workflow](../validation.md) requires a fresh candidate for each release commit and publishes that run's exact tarballs. These results certify the named branch revision; subsequent documentation or merge commits still pass the main release gate before publication. A successful candidate artifact does not itself establish npm publication.

## Distribution and the stable gate

Changesets prerelease mode prepares the `rc` version suffix. The packages retain `publishConfig.tag: experimental`, and the existing release workflow can publish only the tarballs certified by its required validation jobs. Until a successful publication is recorded, use the matching tarballs from the candidate workflow artifact.

Keep [the adoption records](../adoption.md) open until two independent projects and physical RC checks have evidence. A failed walkthrough returns to the responsible roadmap issue. Exiting prerelease mode and promoting the npm channel require a reviewed release change after those gates and maintainer authorization. No evaluator names, private projects or test results are fabricated or published without consent.
