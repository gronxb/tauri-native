# 1.0.0-rc.0 candidate

This is an experimental, reviewable candidate for the source-preserving workflow. It is not a stable 1.0 release or a statement that adoption and physical-device acceptance have completed. Source versions and prepared tarballs do not establish npm publication.

## Included behavior

- Install the CLI in an ordinary Tauri project, inspect its registered commands and export portable iOS/Android binaries, frontend assets and generated TypeScript contracts.
- Consume copied artifacts through React Native, Expo CNG or Lynx, without producer source or Rust tools in the native host build.
- Use ABI 2 nonblocking invocation and explicit cancellation/document/runtime lifetime semantics, with scoped frontend interactions and the verified application-data-directory adaptation.
- Diagnose compatibility and artifact problems, refresh exports incrementally, and watch ordinary source changes.

The [compatibility contract](../compatibility.md) defines supported signatures/APIs and explicit exclusions. The [Fieldnotes walkthrough](../examples/fieldnotes.md) and [migration guide](../migration.md) show the actual installation and host APIs. The producer keeps its ordinary desktop frontend and Rust source.

## Candidate evidence

| Gate | Status |
| --- | --- |
| CLI, RN and Lynx unit/package verification at 1.0.0-rc.0 | Passed locally |
| Receipt rejection for failed/incomplete checks, stale commits and changed tarballs | Passed locally |
| Actual npm dry-run for all three packed packages | Passed locally; explicit `experimental` channel/public access, unchanged tarball hashes |
| Install → both exports → uninstall → normal desktop Release build | Passed locally and in the hosted producer at `4f9b911`; no remaining authored-source changes |
| iOS candidate acceptance at `4f9b911` | Passed: 13 JUnit scenarios; matching producer receipt and package hashes |
| Complete native CI for the final RC commit and matching tarballs | Pending; Android attempt at `4f9b911` failed before runtime acceptance |
| Physical iOS and Android RC execution | Pending |
| Two independent documentation-only onboarding records | Pending |
| Stable publication decision | Pending |

The [validation workflow](../validation.md) must link successful runs for the exact final candidate commit. Earlier Fieldnotes and lifecycle runs establish development evidence for the implementation; they do not substitute for validation of these versioned candidate packages. Update this table only from actual results.

The [hosted producer at `4f9b911`](https://github.com/gronxb/tauri-native/actions/runs/34073619475/job/101595449258) passed Rust, package, compatibility, type, event, watch, desktop and export checks with the packed RC CLI. Its package hashes match the locally prepared publication-dry-run tarballs. The [RC iOS receiving job at 4f9b911](https://github.com/gronxb/tauri-native/actions/runs/34073619475/job/101604256576) passed all 13 JUnit scenarios without failures or skips: original and changed-Rust Fieldnotes in RN/Expo/Lynx, pending navigation in all three hosts, and RN/Lynx async and scoped-view lifetimes. Its receipt matches the transferred producer receipt and all three RC package hashes. The six pending-navigation observations ranged from 2.991 to 14.317 seconds during the injected 20-second Rust delay, including automation overhead.

The [Android receiving attempt](https://github.com/gronxb/tauri-native/actions/runs/34073619475/job/101604256647) passed page-size/storage preflight and all three Release builds/alignment checks, but failed before runtime acceptance: RN/Lynx installation reported a package-service broken pipe; Expo installed but its Maestro driver did not start. The available logs do not establish the cause. This attempt does not certify the complete candidate.

## Distribution and the stable gate

Changesets prerelease mode prepares the `rc` version suffix. The packages retain `publishConfig.tag: experimental`, and the existing release workflow can publish only the tarballs certified by its required validation jobs. Until a successful publication is recorded, use the matching tarballs from the candidate workflow artifact.

Keep [the adoption records](../adoption.md) open until two independent projects and physical RC checks have evidence. A failed walkthrough returns to the responsible roadmap issue. Exiting prerelease mode and promoting the npm channel require a reviewed release change after those gates and maintainer authorization. No evaluator names, private projects or test results are fabricated or published without consent.
